<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vector.uiforlife" >

    <application
        android:name=".application.MainApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".ui.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.PinnedHeaderActivity" />
        <activity android:name=".ui.LoginActivity" />
        <activity android:name=".ui.NetworkActivity" />
        <activity android:name=".ui.CameraActivity" />
        <activity android:name=".ui.ShowImagesActivity" />
        <activity android:name=".ui.TimeDateActivity" />
        <activity android:name=".ui.WidgetActivity" />
        <activity android:name=".ui.DrawerLayoutActivity" />
        <activity android:name=".ui.ListViewActivity" />
    </application>

</manifest>
